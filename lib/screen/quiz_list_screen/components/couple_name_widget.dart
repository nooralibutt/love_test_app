import 'package:love_test_app/utils/all_utilities.dart';

class CoupleNameWidget extends StatelessWidget {
  const CoupleNameWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text('text'),
        Text('text'),
        Text('text'),
      ],
    );
  }
}
